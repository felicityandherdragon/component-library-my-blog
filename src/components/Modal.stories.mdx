import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { ContentModal, LargeContentModal } from './Modal';
import { withKnobs, boolean } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';

<Meta title="Design System|Modals" component={ContentModal} />

# Modals

Modals are dialogs which overlay content to convey.

## Usage

Use modals to call the user to action or complete a task.

## Sign In Modal

The SignInModal is used to prompt the user to sign in to their account.

<Preview withToolbar>
  <Story name="smallModal">
    <ContentModal useSmallModal={boolean("Show small modal", true)} />
  </Story>
</Preview>

## Sign Up Modal

The SignUpModal is used to prompt the user to create an account.

<Preview withToolbar>
  <Story name="largeModal">
    <LargeContentModal useLargeModal={boolean("Show large modal", true)} />
  </Story>
</Preview>
